import { TTodoListItemBar } from './Molecules.types';

const TodoListItemBar = ({ id, done, edit, setEdit, onApprove, onCancel }: TTodoListItemBar) => {
  return <div className="w-full flex items-center justify-end"></div>;
};

export default TodoListItemBar;
